apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-proj-argo
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/firassBenNacib/kube-proj-manifest.git
    targetRevision: HEAD   
    path: meaifest              
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: https://kubernetes.default.svc
  # namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
